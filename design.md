# design.md

`CLAUDE.md` に基づき、コーディング前にページ構成・見た目を定義する。

## デザインコンセプト

- 「ご近所さん歓迎会」らしい、明るく親しみやすい雰囲気にする。
- 新しく守谷市に転入した人が、不安なく必要な情報（日時・会場・申込方法）に辿り着けることを最優先にする。
- モバイルファースト（QRコードはスマートフォンで読み取られる想定）。
- 派手になりすぎない範囲で、守谷市イメージキャラクター「こじゅまる」のカラーを差し色として使い、堅すぎない印象にする。

## カラーパレット

守谷市の公式シンボルカラー「もりやグリーン」は、守谷市CIシステムガイドライン（[101_kihon.pdf](https://www.city.moriya.ibaraki.jp/_res/projects/default_project/_page_/001/004/388/101_kihon.pdf)）に掲載の指定色見本をもとに特定した正式なコードである。こじゅまるの各色は、公式サイトにカラーコードの指定が見当たらなかったため、実際の着ぐるみ写真からの近似値（Web用にイメージを借りたもので、公式のカラーコードではない）。

| 用途 | 色名 | HEX | 使用箇所 |
|---|---|---|---|
| Primary | こじゅまるレッド | `#E6433D` | ヘッダー背景、主要ボタン（申込ボタン等）、見出しアクセント |
| Primary (dark) | こじゅまるレッド（濃） | `#C93830` | ボタンのhover/active、コントラスト確保が必要な文字色 |
| Secondary | もりやグリーン（公式CIカラー） | `#00A985` | サブ見出し、リンク、アクセントライン、フッター |
| Accent 1 | こじゅまるブルー | `#2E7BC7` | アイコン、タイムラインの区切り、情報バッジ |
| Accent 2 | こじゅまるイエロー | `#F0B429` | 強調テキストの下線・マーカー、注意書きバッジ |
| Accent 3 | こじゅまるオレンジ | `#E8821E` | サブボタン、ブース紹介アイコン等の差し色 |
| Neutral | 背景（オフホワイト） | `#FDFBF7` | ページ背景 |
| Neutral | 本文テキスト | `#2B2B2B` | 本文 |
| Neutral | カード背景 | `#FFFFFF` | 情報カード |

- コントラスト確保のため、白文字を乗せるボタン等の主要CTAには濃い方の `#C93830` を優先して使う。
- こじゅまる本人のイラストを掲載する方針に変更（詳細は「こじゅまるイラストの使用方針」セクション参照）。上記のこじゅまるカラーは、あくまでUIパーツ用の参考色であり、実際のイラスト自体の配色を変更することはできない（利用ルールで色の変更が禁止されているため）。

## タイポグラフィ

- 和文本文: OS標準のシステムフォントスタックを使用し、外部フォント読み込みへの依存を避ける。
  `-apple-system, BlinkMacSystemFont, "Hiragino Sans", "Hiragino Kaku Gothic ProN", "Yu Gothic", Meiryo, sans-serif`
- 見出し: 本文と同系統のフォントで、太字（bold）＋やや大きめのサイズでポップさを出す。
- 本文は可読性を優先し、最小16px以上を基準にする。

## ページ構成（1ページ構成、セクション単位）

1. **ヒーロー**: イベント名・キャッチコピー・開催日時（未確定のためプレースホルダー）・申込CTAボタン。背景に2025年開催時の写真（`assets/img/hero.jpg`）を敷き、`background-size: cover`で高さ・縦横比は変えずに横幅に応じて画像が拡大縮小される。テキストは左寄せの`.hero-card`（白、不透明度0.7の半透明背景、角丸）の上に赤文字（`var(--color-primary-dark)`）で配置し、写真に文字が埋もれないようにしている。
2. **イベント概要**: 対象者・参加費・目的の説明文
3. **開催概要**: 日時・会場・受付時間・持ち物を、1つの情報カード内にラベル＋値の一覧（`.fact-list`と同じ形式）で整理（3カラムのカードだと文字がすぐ折り返すため、単一カード＋一覧形式に変更）
4. **当日の流れ**: タイムライン形式（2025年のタイムライン資料を参考にしたプレースホルダー構成）
5. **もりや情報ブース紹介**: 2025年実績のブース一覧を参考にした簡潔なリスト
6. **申込方法**: Googleフォームへのボタン、締切日（2026-09-10、フォームURL確定済みのため実リンクに差し替え済み）
7. **昨年の様子**: 2025年開催時の写真6枚（`assets/img/photo-01.png`〜`photo-06.png`）を2列（PC/タブロイド幅では3列）グリッドで掲載。各写真はキャプション付きで、クリック（タップ）するとオーバーレイのライトボックスで拡大表示できる（`assets/js/gallery.js`）
8. **アクセス・問い合わせ先**
9. **フッター**: 主催表記（QRコード自体の掲載は不要。QR経由での流入前提のため）

※やさしい日本語／英語セクションは一度実装したが、ユーザーの指示により削除した（多言語対応は今回は行わない方針）。

## UIパーツの方針

- ボタン: 角丸、こじゅまるレッド背景・白文字、タップ領域は44px以上を確保
- 情報カード: 白背景・薄い影・角丸。開催概要は単一カード内にラベル＋値の一覧（`.fact-list`）を配置する形式
- タイムライン: 縦線＋ドットで時系列を表現し、Accent系の色（ブルー・イエロー・オレンジ）を交互に使用
- **フローティング申込ボタン（実装済み、2026-09-11）**: 画面右下に`position: fixed`の丸ボタン（`.fab-apply`）を常時表示し、スクロール位置によらずGoogleフォームへすぐ遷移できるようにしている。色・角丸はボタン方針と統一（こじゅまるレッド濃・白文字・角丸pill）。iOSのホームバー領域と重ならないよう`env(safe-area-inset-bottom)`を考慮。

## レスポンシブ方針

- 375px幅を基準に、モバイルでは1カラムレイアウト
- 480px以上の画面では左右余白を増やし、最大幅600〜680px程度で中央寄せ表示にする
- 複雑なグリッドレイアウトは組まず、シンプルな縦積み構成を維持する

## こじゅまるイラストの使用方針

守谷市公式サイトの利用ルール（`CLAUDE.md` のオープン事項参照）に従い、こじゅまる本人のイラストを掲載する。素材は `こじゅまるイラスト/` フォルダ内から選定し、実装時に `assets/img/kojumaru/` 配下へ配置する（具体的にどのファイルをどこで使うかはユーザーが別途指定する）。

- **ヒーロー部分に1点**: ページ最上部のヒーローセクションに、正面向きの立ち姿など「顔となるカット」を1点、目立つサイズで配置する。テキスト（キャッチコピー・CTAボタン）を隠さないよう、モバイルではテキストの下または横に添える形とし、幅480px以上ではテキストの右側に並べるレイアウトも検討する。
- **背景装飾として小さく点在**: 各セクションの背景に、同じイラスト（または数種類）を小さく・低い主張度で複数点在させる。以下の方針で「うるさくならない」よう配慮する。
  - サイズは1点あたり40〜80px程度の小サイズにとどめる
  - 不透明度を下げる（目安 opacity: 0.12〜0.2程度）か、セクション背景色に馴染む程度の主張度にする
  - `position: absolute` + `pointer-events: none` で本文レイアウトやタップ操作に影響しないようにする
  - 本文・カード・ボタンの上には重ねない（あくまで余白部分に配置する）
  - 出現数はページ全体で5〜8点程度を目安にし、セクションごとに1〜2点まで

  **実装済み（2026-09-09、その後ユーザー指示で調整）**: 各`.section`の四隅（本文が来ない上下パディング帯）に、`.deco-kojumaru`クラス（width:56px、opacity:0.7、position:absolute、pointer-events:none）で配置。ヒーロー・お申込み方法・お問合せの3セクションは視認性優先のため装飾なし。720px以上の幅では、コンテナの最大幅（680px）に合わせてコンテンツ列の端から16px外側に位置を追従させ、画面端に寄りすぎないようにしている（`calc((100% - 680px) / 2 + 16px)`）。

  | セクション | 位置 | 使用イラスト |
  |---|---|---|
  | このイベントについて (#about) | 右上 | `kojumaru002.png` |
  | 開催概要 (#overview) | 左下 | `kojumaru004.png` |
  | 当日の流れ (#timeline) | 右上 | `kojumaru017.png` |
  | もりや情報ブース (#booths) | 右上 | `kojumaru_31.png` |
  | 昨年の様子 (#gallery) | 右上 | `kojumaru002.png`（再利用） |
  | アクセス (#access) | 左下 | `kojumaru004.png`（再利用） |

  ヒーロー部分に大きく1点掲載する件は、上記「ヒーロー部分に1点」の方針のままユーザーからの具体的な指定待ち（未実装）。
- **改変の禁止事項を厳守**: 縦横比を変えない、反転しない、過度なトリミングをしない、色を変えない（CSSでの色変更・フィルターも不可）。サイズ変更（拡大縮小）自体は禁止事項に含まれないため、上記の小型化・背景装飾利用は問題ない。
- **クレジット表記**: ページ内に1箇所、「守谷市イメージキャラクター こじゅまる」の表記を掲載する（フッター付近を想定）。ヒーロー・背景装飾を含むページ全体の利用に対する表記として扱う。

## アセット方針

- QRコード画像: `assets/img/qr.png`（サイト公開URL確定後に生成し配置）
- アイコン: 絵文字またはシンプルなインラインSVGで代用し、外部アイコンライブラリには依存しない
- こじゅまるイラスト: `assets/img/kojumaru/`（上記「こじゅまるイラストの使用方針」参照）

## 検索避け（2026-09-09導入、2026-09-18にパスワードゲートは廃止）

- **検索エンジン対策**: `<meta name="robots" content="noindex, nofollow">` をheadに追加し、`robots.txt`で全パスを`Disallow`。これによりGoogle等の検索結果には表示されなくなる（ただし直接URLを知っていればアクセス自体は可能）。
- ~~クライアントサイドのパスワードゲート~~（2026-09-18にユーザー指示で廃止）: 以前は`#auth-gate`（ID・パスワード入力フォーム）と`#site-content`（本来のページ内容）の2つをbody直下に配置し、`assets/js/auth.js`でSHA-256パスワードチェックを行っていたが、削除した。現在はページにアクセスすれば即座に内容が表示される。

## アクセス解析（GA4）とコンバージョン計測（2026-09-14）

PV（ページビュー）と申込導線のクリックを計測するため、GA4（Google Analytics 4）を直接埋め込み方式（gtag.js）で導入する。GTM（Google Tag Manager）は、このサイトが1ページ・少数タグの静的サイトであるため採用せず、シンプルさを優先した。

- **PV計測**: `index.html`の`<head>`にgtag.jsスニペットを設置（`<meta name="robots">`の直後）。GA4の自動計測（Enhanced measurement）により`page_view`イベントが自動送信される。
- **測定ID**: `G-L8WZNQQLK7`（2026-09-14に確定・設定済み）。GA4プロパティ作成・測定ID取得の手順は`log.md`の2026-09-14の記録を参照。
- **コンバージョン計測（間接指標）**: 申込はGoogleフォーム（別タブで開く外部サイト）で完結するため、このサイト側から「フォーム送信完了」は直接検知できない（クロスオリジンのため）。代わりに、申込導線となる3つのリンク（ヒーローの`.hero-cta`、お申込み方法セクションの`.btn-primary`、フローティングボタンの`.fab-apply`）すべてに共通クラス`js-apply-cta`と、設置箇所を区別する`data-cta-position`属性（`hero` / `apply_section` / `floating_button`）を付与。`assets/js/analytics.js`がこれらのクリック時にGA4カスタムイベント`apply_click`（パラメータ`cta_position`）を送信する。
  - **注意**: これは「Googleフォームへの遷移」を計測する近似値であり、フォームの実際の送信完了を意味しない。GA4管理画面側で`apply_click`イベントを「コンバージョンとしてマーク」する設定（ユーザー側の作業、コード側では完結しない）を行うことで、GA4のコンバージョンレポートに反映される。

## アクセシビリティ

- 本文文字サイズ16px以上
- ボタン等のタップ領域は44px以上
- 主要な文字色・背景色の組み合わせはWCAG AA相当のコントラスト比を意識する（特に赤背景に白文字を乗せる箇所は `#C93830` を使用）
